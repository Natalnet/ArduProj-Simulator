import styled from 'styled-components';

export const ButtonStatusAddComponent = styled.button`
  position: absolute;
  top: 1rem;
  left: 1rem;
  cursor: pointer;
  padding: .5rem .75rem;
  border-radius: .5rem;
  color: var(--letter-color);
  background-color: var(--button-color);
  border:none;
  outline: none;
  z-index: 1;
`;