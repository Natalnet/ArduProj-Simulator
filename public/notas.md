# Tasks da semana
- trocar o led pra começar desligado  FEITO
- descrição textual (importar o circuito completo) INICIADO  --- Problema com renderizar as linhas provavelmente ligado as sections no makeline()
- arrumar a gambiarra do value 0  FEITO
- fazer os eventos alternarem os valores em vez de setarem FEITO

# Perguntar a julio sobre
- O loop que é gerado pelo botão e como lidar 
- Em que momento eu deveria chamar o led para ele se atualizar

# Tasks da semana
- guardar o caminho no output para evitar que o mesmo componente seja chamado duas vezes 
- usar o value:0 para chamar os componentes que não estão passando energia
- descrição textual (importar o circuito completo) INICIADO  --- Problema com renderizar as linhas provavelmente ligado as sections no makeline()
